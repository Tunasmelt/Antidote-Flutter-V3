# Server Configuration
PORT=5000
NODE_ENV=development

# Spotify Configuration
# Only Client ID is needed (public, safe to use)
# Client Secret is NO LONGER NEEDED - users authenticate with their own accounts
SPOTIFY_CLIENT_ID=your_spotify_client_id_here

# Database Configuration (if using)
# DATABASE_URL=your_database_url

# Supabase Configuration
# Get these from your Supabase project settings: https://app.supabase.com/project/_/settings/api
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
# Note: Use SERVICE_ROLE_KEY (not anon key) for backend - it bypasses RLS for admin operations

